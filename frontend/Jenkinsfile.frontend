pipeline {
    agent any

    environment {
        HARBOR_URL = '192.168.56.200:30002'
        HARBOR_PROJECT = 'alphacar'
        IMAGE_NAME = 'frontend'
        CREDENTIAL_ID = 'harbor-cred'
        SONAR_TOKEN_ID = 'sonarqube-token'
    }

    stages {
        stage('Prepare') {
            steps {
                script {
                    def baseVer = "1.0"
                    try {
                        baseVer = readFile('frontend/version.txt').trim()
                    } catch (e) {
                        echo "⚠️ version.txt 없음"
                    }
                    env.FULL_VERSION = "${baseVer}.${currentBuild.number}"
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                script {
                    def scannerHome = tool 'sonar-scanner'
                    withCredentials([string(credentialsId: "${SONAR_TOKEN_ID}", variable: 'S_TOKEN')]) {
                        // exclusions 추가로 속도 대폭 개선
                        sh """
                        ${scannerHome}/bin/sonar-scanner \
                        -Dsonar.projectKey=alphacar-frontend \
                        -Dsonar.sources=frontend \
                        -Dsonar.host.url=http://192.168.56.200:32001 \
                        -Dsonar.login=${S_TOKEN} \
                        -Dsonar.exclusions=**/node_modules/**,**/.next/**,**/dist/**
                        """
                    }
                }
            }
        }

        stage('Docker Build & Push') {
            steps {
                script {
                    // 🚀 [핵심 수정] 변수 계산 대신 젠킨스 도커 플러그인 전용 문법 사용
                    // 'docker'라는 이름은 Tools 메뉴에 등록한 Name과 똑같아야 합니다.
                    docker.withTool('docker') {
                        echo "🔨 이미지 빌드 중: ${env.FULL_VERSION}"
                        
                        // 1. 빌드
                        def customImage = docker.build("${HARBOR_URL}/${HARBOR_PROJECT}/${IMAGE_NAME}:${env.FULL_VERSION}", "-f frontend/Dockerfile frontend/")
                        
                        // 2. 하버 푸시
                        docker.withRegistry("http://${HARBOR_URL}", "${CREDENTIAL_ID}") {
                            customImage.push()
                        }
                        
                        // 3. 로컬 이미지 정리
                        sh "docker rmi ${HARBOR_URL}/${HARBOR_PROJECT}/${IMAGE_NAME}:${env.FULL_VERSION}"
                    }
                }
            }
        }
    }

    post {
        success { echo "✅ 모든 공정 성공! 하버에서 확인하세요." }
        failure { echo "❌ 빌드 실패! 로그의 상단 에러 메시지를 확인하세요." }
    }
}
